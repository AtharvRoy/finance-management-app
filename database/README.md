# Database Migrations

Default target: PostgreSQL.

## Run migrations (example)

Use your preferred migration runner, or execute SQL manually in order:

1. `migrations/001_init.sql`

For local-only MVP, you can adapt these scripts to SQLite and point `DATABASE_URL` accordingly.
